<?xml version="1.0"?>
<ruleset name="Quick 2FA Standards">
	<description>WordPress Coding Standards for Quick 2FA</description>
	
	<file>.</file>
	
	<!-- Exclude patterns -->
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/assets/*</exclude-pattern>
	<exclude-pattern>*/.git/*</exclude-pattern>
	<exclude-pattern>*/dev-notes/*</exclude-pattern>
	<exclude-pattern>*/docs/*</exclude-pattern>
	
	<!-- Arguments -->
	<arg value="sp"/>
	<arg name="basepath" value="."/>
	<arg name="colors"/>
	<arg name="extensions" value="php"/>
	<arg name="parallel" value="8"/>
	
	<!-- Rules -->
	<rule ref="WordPress">
		<exclude name="Generic.Arrays.DisallowShortArraySyntax"/>

		<!-- An if with elseif branches ends in an explicit else, with no-op branches
		     holding only a comment (see CLAUDE.md, PHP Style). This sniff counts a comment as
		     empty, so only its if/elseif/else codes are excluded; empty catch, loop and
		     switch bodies are still errors. -->
		<exclude name="Generic.CodeAnalysis.EmptyStatement.DetectedIf"/>
		<exclude name="Generic.CodeAnalysis.EmptyStatement.DetectedElseif"/>
		<exclude name="Generic.CodeAnalysis.EmptyStatement.DetectedElse"/>
	</rule>
	
	<!-- Nonce verification is wrapped in verify_page_nonce() (functions-private.php),
	     so the sniff cannot see the wp_verify_nonce() call inline. Declaring the
	     wrapper keeps the sniff working, rather than sprinkling phpcs:ignore at
	     every $_POST read and suppressing genuine findings along with it. -->
	<rule ref="WordPress.Security.NonceVerification">
		<properties>
			<property name="customNonceVerificationFunctions" type="array">
				<element value="verify_page_nonce"/>
				<element value="verify_admin_action_nonce"/>
			</property>
		</properties>
	</rule>

	<!-- Prefixes -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="quick_2fa"/>
				<element value="quick2fa"/>
				<element value="q2fa"/>
				<element value="Quick_2FA"/>
				<element value="QUICK_2FA"/>
			</property>
		</properties>
	</rule>
</ruleset>
